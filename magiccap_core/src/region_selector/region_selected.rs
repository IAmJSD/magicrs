use super::{engine::RegionSelectorContext, RegionCapture};

// Handles capturing the region.
pub fn region_capture(
    ctx: &mut RegionSelectorContext, i: usize, x: i32, y: i32, w: i32, h: i32,
) -> Option<RegionCapture> {
    // TODO
    None
}
